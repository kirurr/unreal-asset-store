RewriteEngine On

# Если файл или папка существует, отдать их напрямую
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Перенаправление всех запросов на index.php
RewriteRule ^(.*)$ index.php [QSA,L]

